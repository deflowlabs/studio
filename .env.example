# Required public configuration. SANITY_STUDIO_* values are embedded in the
# browser bundle; never place tokens or secrets in them.
SANITY_STUDIO_PROJECT_ID=i34vbeac
SANITY_STUDIO_DATASET=production
SANITY_STUDIO_API_VERSION=2026-08-17

# Dedicated website used by Presentation/live preview. Production Studio must
# never point to the public website because draft responses must not enter ISR.
SANITY_STUDIO_PREVIEW_URL=http://localhost:3000

# Studio's own stable origin. It is displayed as workspace context and must
# match the Vercel domain in production.
SANITY_STUDIO_HOST=http://localhost:3333

# Technical GROQ query tool; keep false for normal and production editors.
SANITY_STUDIO_ENABLE_VISION=false
